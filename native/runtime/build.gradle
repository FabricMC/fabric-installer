subprojects {
	apply plugin: "maven-publish"

	version = "0.2.0"

	publishing {
		publications {
			create("maven", MavenPublication) {
				groupId "net.fabricmc.fabric-installer-native-lib.${project.name}"
				artifactId project.name
				version project.version
			}
		}
	}
}

